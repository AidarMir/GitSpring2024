package CodingBat.Warmap1;

public class startHi {
    public boolean startHi(String str) {

        if (str.startsWith("hi")) {
            return true;
        }return false;
    }
// Учитывая строку, верните true,
// если строка начинается с «привет», и false в противном случае.
}
