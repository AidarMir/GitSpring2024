package CodingBat.Warmap1;

public class in1020 {
    public boolean in1020(int a, int b) {

        return (a >= 10 && a <= 20 || b >= 10 && b <= 20) ? true : false;
    }

// Учитывая два целочисленных значения, верните true,
// если любое из них находится в диапазоне 10..20 включительно.
}
